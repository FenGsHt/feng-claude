node_modules
dist
dist-build
dist-build-tmp-*
out
.DS_Store
*.local
src/main/claudeEnv.ts

# 仓库内个人覆盖（复制 example 为对应 .local.json 并按需填写；勿提交）
local/claude.local.json
local/deploy-config.local.json

.claude-session/
.claude/settings.local.json

# 根目录便携 exe（npm run build:root-exe；启用 .githooks 后每次 commit 也会生成）
/claude-gui-latest.exe
/claude-gui-*-portable-*.exe
